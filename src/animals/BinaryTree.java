package animals;

public class BinaryTree {
    // Root of Binary Tree
    Node root;

    // Constructors
    BinaryTree(String key) {
        root = new Node(key);
    }

    BinaryTree() {
        root = null;
    }

}
